TRIGGER TGR_0REBATE_GRP
AFTER INSERT ON STG_0REBATE_GRP
FOR EACH STATEMENT 
BEGIN 

	MERGE INTO DIM_MATERIAL M
	USING  STG_0REBATE_GRP S ON M."CD_GRUPORAPPEL" = S."BONUS" AND  SPRAS='S'
	WHEN MATCHED THEN UPDATE 
	SET 	M."TX_GRUPORAPPEL" =S.VTEXT;

END