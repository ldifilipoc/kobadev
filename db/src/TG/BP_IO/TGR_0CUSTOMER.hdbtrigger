TRIGGER "TGR_0CUSTOMER"
AFTER INSERT ON "STG_0CUSTOMER_TEXT" 
FOR EACH STATEMENT 
BEGIN 

	merge into DIM_BPARTNER DM
	using STG_0CUSTOMER_TEXT S ON S.KUNNR = DM.CD_BP
	when matched then update set NM_BP = S.TXTMD, TIMESTMP =CURRENT_TIMESTAMP;

END